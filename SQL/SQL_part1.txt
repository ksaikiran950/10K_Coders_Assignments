1. select ename from where sal+(sal*10/100)> 3000;

2. select ename,(sal*12*1.10) as annual_salary from emp where mgr=7566;

3. select * from emp where ename like 'k%' and ename like '%s';

4. select * from emp where substr(hiredate,3,2) between 80 and 87 and substr(ename,1,1) not in ('a','e','i','o','u');

5. select * from emp where substr(ename,1,1) in ('a','e','i','o','u') and substr(ename,-1,1) not in ('a','e','i','o','u');

6. select ename from emp where job='manager';

7. select empno,ename from emp where comm is not null;

8. select ename  from emp where deptno in (10,20);

9. select ename from emp where job ='clerk' and sal>3000;

10. select ename from emp where job in ('clerk','salesman','analyst','drawing') and sal >2500;

11. select * from emp where year(hiredate)<1980 or year(hiredate)>1988;

12. select ename from emp where deptno in (10,20,40) or job in ('clerk','salesman','analyst');

13. select ename from emp where ename like 's____';

14. select ename from emp where comm is null and deptno in  (10,20);

15. select *,(sal*12) as annual_sal from emp where job='manager' and  deptno in (10,20) and (sal*12)>12000;

16. select * from emp where month(hiredate)=02;

17. select *,(sal*12) 'annual_salary' from emp where ename  like 'm%' and ename like '%s';

18. select ename,hiredate from emp where hiredate between '1981-01-01' and '1981-12-31';

19. select * from emp where job = 'salesman';

20. select * from emp where deptno in (10,20);

21. select *, (sal*6) as 'half_term_sal' fro emp where (sal*6)>5000 and substr(ename,1,1) in ('a','e','i','o','u');

22. select *  from emp where job!='manager' and sal between 1000 and 2000;

23. select * from emp where job in ('clerk','president');

24. select * from emp where sal between 0 and 1400;

25. select ename from emp where deptno=10 and ename like '_m%';

26. select max(sal),deptno from emp where deptno in(10,20) group by deptno;

27. select ename from emp where sal<(select sal from emp where ename='smith';

28. select min(sal),deptno from emp group by deptno;

29. select dname,count(*) from emp group by deptno having count(*)>=3 and count(*)<=5;

30. select deptno,count(*) from emp where job!='salesman' group by deptno;

31. select *,count(*) from emp group by sal having count(*)>1;

32. select ename,count(*) from emp group by ename having count(*)>1;

33. select avg(sal),deptno from emp where deptno!=10 group by deptno;

34.select ename,(sal+comm) as "total_sal" from emp where sal>2500;

35. select job,avg(sal) from emp group by  sal having sal between 30000 and 60000;

36. select * from emp order by deptno asc,sal desc;

37. select * from emp order by job asc,ename asc;

38. select sal from emp where sal>30000 order by sal desc;

39. select * from emp where deptno=10 order by hiredate asc;

40 select ename,job from emp orer by job desc,sal asc;

